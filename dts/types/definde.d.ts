export declare namespace DEFINDE {
    const BC_USER_NUM: number;
    const BC_PORT_STR_LEN: number;
    const BC_MAX_NAME_LEN: number;
    const BC_MAX_PWD_LEN: number;
    const BC_SERIALNO_LEN: number;
    const BC_MACADDR_LEN: number;
    const BC_MAX_ADDR_LEN: number;
    const BC_MAX_NICKNAME_LEN: number;
    const BC_TYPE_INFO_LEN: number;
    const BC_BUILD_INFO_LEN: number;
    const BC_VERSION_INFO_LEN: number;
    const BC_DETAIL_INFO_LEN: number;
    const BC_MAX_ALARMOUT: number;
    const BC_MAX_ALARMIN: number;
    const BC_MAX_CHANNEL: number;
    const BC_MAX_RF_NUM_V20: number;
    const BC_MAX_DISKNUM: number;
    const BC_MAX_ENC_PROFILE_NUM: number;
    const BC_LOG_INFO_LEN: number;
    const BC_MAX_FRAME_SIZE: number;
    const BC_MAX_CONFIG_BUF_SIZE: number;
    const BC_MAX_DAYS: number;
    const BC_MAX_TIMESEGMENT: number;
    const BC_MAX_COVER_AREA_NUM: number;
    const BC_MAX_MOTION_SENS_NUM: number;
    const BC_MAX_FRAME_RATE_NUM: number;
    const BC_MAX_BIT_RATE_NUM: number;
    const BC_MAX_UID_LEN: number;
    const BC_MAX_DDNS_NUM: number;
    const BC_MAX_IPFILTER_NUM: number;
    const BC_MAX_FILE_LEN: number;
    const BC_MAX_RESOLUTION_NUM: number;
    const BC_MAX_DWELL_NUM: number;
    const BC_MAX_AUTHORITY_NUM: number;
    const BC_MAX_POS_NUM: number;
    const BC_MAX_KEY_POS: number;
    const BC_MAX_CRUISE_NUM: number;
    const BC_MAX_TRACK_NUM: number;
    const BC_MAX_TRACK_SIZE: number;
    const BC_WIFI_UDID_MAX_NUM: number;
    const MAX_UINT32_NUM: number;
    const MAX_INT32_NUM: number;
    const NET_MAX_SHELTERNUM: number;
    const NET_MAX_ALARMOUT: number;
    const NET_MAX_DAYS: number;
    const NET_MAX_TIMESEGMENT: number;
    const NET_MAX_CHANNEL_NUM: number;
    const NET_MAX_ABILITY_BYTE_NUM: number;
    const SDK_MAX_DEVICE_NUM: number;
    const SDK_MAX_NAME_LEN: number;
    const SDK_MAX_PASSWD_LEN: number;
    const SDK_MAX_HOSTNAME_LEN: number;
    const SDK_MAX_NORMAL_STR_LEN: number;
    const SDK_MAX_UID_STR_LEN: number;
    const SDK_MAX_MAC_STR_LEN: number;
    const SDK_MAX_FILENAME_LEN: number;
}
